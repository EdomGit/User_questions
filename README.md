# Question Generator API

FastAPI приложение для генерации вопросов на основе содержимого веб-страниц с использованием OpenAI API.

## 📋 Описание

Question Generator API - это RESTful веб-сервис, который принимает URL веб-страницы и возвращает список из 5 вопросов, сгенерированных на основе содержимого страницы с помощью OpenAI API.

### Основные возможности:
- 📄 Извлечение текстового содержимого из веб-страниц
- 🤖 Генерация вопросов с помощью OpenAI API
- 🐳 Развертывание через Docker
- 📚 Интерактивная документация API (Swagger UI)

## 🚀 Быстрый старт

### Вариант 1: Запуск через Docker Compose (Рекомендуется)

1. **Клонируйте репозиторий:**
   ```bash
   git clone <repository-url>
   cd User_questions
   ```

2. **Создайте файл `.env` в корне проекта:**
   ```env
   OPENAI_API_KEY=your_api_key_here
   OPENAI_BASE_URL=https://api.openai.com/v1
   ```

3. **Запустите приложение:**
   ```bash
   docker-compose up -d
   ```

4. **Проверьте работу API:**
   - Откройте браузер: http://localhost:8001
   - Документация API: http://localhost:8001/docs

### Вариант 2: Локальная установка

#### Требования
- Python 3.12 или выше
- pip (менеджер пакетов Python)
- OpenAI API ключ

#### Установка

1. **Создайте виртуальное окружение:**
   ```bash
   python -m venv venv
   ```

2. **Активируйте виртуальное окружение:**
   
   **Windows (PowerShell):**
   ```powershell
   .\venv\Scripts\Activate.ps1
   ```
   
   **Windows (CMD):**
   ```cmd
   venv\Scripts\activate.bat
   ```
   
   **Linux/Mac:**
   ```bash
   source venv/bin/activate
   ```

3. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

4. **Создайте файл `.env` в корне проекта:**
   ```env
   OPENAI_API_KEY=your_api_key_here
   OPENAI_BASE_URL=https://api.openai.com/v1
   ```

5. **Запустите приложение:**
   ```bash
   python main.py
   ```
   
   Или через uvicorn напрямую:
   ```bash
   uvicorn main:app --host 0.0.0.0 --port 8001 --reload
   ```

6. **Проверьте работу API:**
   - Откройте браузер: http://localhost:8001
   - Документация API: http://localhost:8001/docs

## 🐳 Развертывание через Docker

### Сборка образа вручную

1. **Соберите Docker-образ:**
   ```bash
   docker build -t question-generator-api:latest .
   ```

2. **Запустите контейнер:**
   ```bash
   docker run -d \
     --name question-generator-api \
     -p 8001:8001 \
     --env-file .env \
     question-generator-api:latest
   ```

### Использование готового образа из Docker Hub

Образ доступен в Docker Hub: `edom09/question-generator-api:latest`

1. **Создайте файл `.env` с вашими переменными окружения**

2. **Запустите контейнер:**
   ```bash
   docker run -d \
     --name question-generator-api \
     -p 8001:8001 \
     --env-file .env \
     edom09/question-generator-api:latest
   ```

3. **Или используйте docker-compose:**
   
   Обновите `docker-compose.yml`:
   ```yaml
   services:
     question-generator-api:
       image: edom09/question-generator-api:latest
       container_name: question-generator-api
       ports:
         - "8001:8001"
       env_file:
         - .env
       restart: unless-stopped
   ```
   
   Затем запустите:
   ```bash
   docker-compose up -d
   ```

## 🚢 Деплой в Docker Hub

### Ручной деплой в Docker Hub

Если вы хотите выполнить деплой вручную:

1. **Соберите образ:**
   ```bash
   docker build -t question-generator-api:latest .
   ```

2. **Тегируйте образ для Docker Hub:**
   ```bash
   docker tag question-generator-api:latest edom09/question-generator-api:latest
   ```

3. **Авторизуйтесь в Docker Hub:**
   ```bash
   docker login -u edom09
   ```

4. **Отправьте образ в Docker Hub:**
   ```bash
   docker push edom09/question-generator-api:latest
   ```

## 📝 Конфигурация

### Переменные окружения

Создайте файл `.env` в корне проекта со следующими переменными:

| Переменная | Описание | Обязательная | По умолчанию |
|------------|----------|--------------|--------------|
| `OPENAI_API_KEY` | API ключ OpenAI | Да | - |
| `OPENAI_BASE_URL` | Базовый URL OpenAI API | Нет | `https://api.openai.com/v1` |

**Пример `.env` файла:**
```env
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
OPENAI_BASE_URL=https://api.openai.com/v1
```

### Порт приложения

По умолчанию приложение запускается на порту **8001**. Чтобы изменить порт:

- **В Docker Compose:** измените маппинг портов в `docker-compose.yml`
- **При локальном запуске:** измените параметр `--port` в команде uvicorn или в `main.py`

## 📚 Использование API

### Endpoints

#### `GET /`
Корневой endpoint с информацией об API.

**Ответ:**
```json
{
  "message": "Question Generator API",
  "version": "1.0.0",
  "description": "API для генерации вопросов на основе содержимого веб-страниц",
  "endpoints": {
    "POST /generate-questions": "Генерация вопросов из URL веб-страницы",
    "GET /docs": "Интерактивная документация API (Swagger UI)",
    "GET /redoc": "Альтернативная документация API (ReDoc)"
  }
}
```

#### `POST /generate-questions`
Генерирует вопросы на основе содержимого веб-страницы.

**Запрос:**
```json
{
  "url": "https://example.com/article"
}
```

**Ответ:**
```json
{
  "questions": [
    "Какой основной вопрос рассматривается в статье?",
    "Какие ключевые моменты выделены?",
    "Какие примеры приведены?",
    "Какие выводы можно сделать?",
    "Какие вопросы остались открытыми?"
  ]
}
```

### Примеры использования

#### cURL
```bash
curl -X POST "http://localhost:8001/generate-questions" \
  -H "Content-Type: application/json" \
  -d '{"url": "https://example.com/article"}'
```

#### Python
```python
import requests

response = requests.post(
    "http://localhost:8001/generate-questions",
    json={"url": "https://example.com/article"}
)
questions = response.json()["questions"]
print(questions)
```

#### JavaScript (fetch)
```javascript
const response = await fetch('http://localhost:8001/generate-questions', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
  },
  body: JSON.stringify({
    url: 'https://example.com/article'
  })
});

const data = await response.json();
console.log(data.questions);
```

### Интерактивная документация

После запуска приложения доступна интерактивная документация:
- **Swagger UI:** http://localhost:8001/docs
- **ReDoc:** http://localhost:8001/redoc

## 🔧 Управление контейнерами

### Просмотр логов
```bash
docker-compose logs -f question-generator-api
```

Или для отдельного контейнера:
```bash
docker logs -f question-generator-api
```

### Остановка контейнера
```bash
docker-compose down
```

Или для отдельного контейнера:
```bash
docker stop question-generator-api
docker rm question-generator-api
```

### Перезапуск контейнера
```bash
docker-compose restart question-generator-api
```

### Просмотр статуса
```bash
docker-compose ps
```

### Пересборка и перезапуск
```bash
docker-compose up -d --build
```

## 🧪 Тестирование

### Запуск тестов модуля
```bash
python test_openai_module.py
```

### Использование модуля напрямую
```python
from openai_module import get_questions_from_text

questions = get_questions_from_text("Ваш текст здесь")
print(questions)
```

## 🐛 Устранение неполадок

### Проблема: Контейнер не запускается

**Решение:**
1. Проверьте логи: `docker-compose logs question-generator-api`
2. Убедитесь, что файл `.env` существует и содержит `OPENAI_API_KEY`
3. Проверьте, что порт 8001 не занят другим приложением

### Проблема: Ошибка "OPENAI_API_KEY not found"

**Решение:**
1. Убедитесь, что файл `.env` находится в корне проекта
2. Проверьте, что переменная `OPENAI_API_KEY` указана в файле `.env`
3. При использовании Docker Compose убедитесь, что `env_file: - .env` указан в `docker-compose.yml`

### Проблема: Ошибка при загрузке веб-страницы

**Решение:**
1. Проверьте, что URL доступен и корректный
2. Убедитесь, что сайт не требует авторизации
3. Проверьте интернет-соединение

### Проблема: Порт уже занят

**Решение:**
1. Измените порт в `docker-compose.yml` (например, на `8002:8001`)
2. Или остановите приложение, использующее порт 8001

### Проблема: Ошибка при деплое в Docker Hub

**Решение:**
1. Убедитесь, что вы авторизованы: `docker login -u edom09`
2. Проверьте, что у вас есть права на запись в репозиторий `edom09/question-generator-api`
3. Проверьте интернет-соединение

## 📦 Структура проекта

```
User_questions/
├── agent.py                 # Логика генерации вопросов
├── main.py                  # FastAPI приложение
├── openai_module.py         # Модуль работы с OpenAI API
├── test_openai_module.py    # Тесты модуля
├── requirements.txt         # Зависимости Python
├── Dockerfile              # Docker образ
├── docker-compose.yml      # Docker Compose конфигурация
├── deploy.bat              # Скрипт деплоя
├── .env                    # Переменные окружения (создать вручную)
└── README.md               # Документация
```

## 🔐 Безопасность

- ⚠️ **Никогда не коммитьте файл `.env` в репозиторий!**
- ✅ Убедитесь, что `.env` добавлен в `.gitignore`
- ✅ Используйте сильные API ключи
- ✅ Ограничьте доступ к API в production окружении

## 📄 Лицензия

[Укажите лицензию проекта]

## 👥 Авторы

Евгений Могильников

## 🤝 Вклад в проект

[Укажите информацию о том, как можно внести вклад в проект]
